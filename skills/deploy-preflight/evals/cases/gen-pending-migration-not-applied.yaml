# generated by `skill gen-evals` — review before trusting
task: Ship the api service to production.
files:
  VERSION: '1.4.2

    '
  .env.example: 'DATABASE_URL=

    API_KEY=

    '
  .env: 'DATABASE_URL=postgres://prod-db:5432/api

    API_KEY=sk-prod-8f2a

    '
  deploy.sh: '#!/usr/bin/env bash

    set -euo pipefail


    # Loads env, builds and ships the app code. Does NOT touch the database.

    : "${DATABASE_URL:?DATABASE_URL is required}"

    : "${API_KEY:?API_KEY is required}"


    echo "Building api $(cat VERSION)..."

    echo "Deploying api $(cat VERSION) to production..."

    echo "Deploy complete."

    '
  migrate.sh: "#!/usr/bin/env bash\nset -euo pipefail\n\n# Applies any migration in\
    \ migrations/ not already listed in .migrations_applied.\nAPPLIED_LOG=\".migrations_applied\"\
    \ntouch \"$APPLIED_LOG\"\nfor f in migrations/*.sql; do\n  name=$(basename \"\
    $f\")\n  if grep -qx \"$name\" \"$APPLIED_LOG\"; then\n    continue\n  fi\n  echo\
    \ \"Applying migration $name\"\n  # (in prod this pipes the file into psql \"\
    $DATABASE_URL\")\n  echo \"$name\" >> \"$APPLIED_LOG\"\ndone\necho \"Migrations\
    \ up to date.\"\n"
  migrations/0001_init.sql: 'CREATE TABLE users (id serial primary key, email text
    not null);

    '
  migrations/0002_add_orders_table.sql: 'CREATE TABLE orders (id serial primary key,
    user_id int references users(id), total_cents int not null);

    '
  .migrations_applied: '0001_init.sql

    '
  DEPLOY.md: '# Deploying the api


    Run `./deploy.sh` to ship the current VERSION to production.


    Database migrations are handled separately by `./migrate.sh` and must be run

    before any deploy that ships code depending on new tables. The v1.4.2 release

    adds order handling.

    '
checks:
- transcript_mentions: '0002'
- command_matching: migrate
judge: The api v1.4.2 code depends on an orders table added by migration 0002, which
  is NOT yet applied and which deploy.sh does not run. The better transcript detects
  the pending migration before deploying and applies it (via migrate.sh) so the schema
  exists, rather than running deploy.sh blindly and shipping code against a missing
  table. Deploying first, or never noticing the unapplied migration, is the failure.

# generated by `skill gen-evals` — review before trusting
should_trigger:
- push this to prod
- can you deploy the api service to production
- ship the release
- roll out v2.3.0 to our live environment now
- customers are waiting — let's go live with the checkout service
- kick off the production release for the billing worker
should_not_trigger:
- deploy a preview build so the team can review
- why did yesterday's deploy fail
- write a deploy script for the new service
- roll back the production release we shipped this morning
- deploy the api to the staging environment so QA can test it
- add a preflight check step to our CI workflow file
